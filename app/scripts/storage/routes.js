'use strict';

// file exists to define 'auto.storage' module
// there are no routes associated with this component.
angular.module('auto.storage', [])
  .config([
    function() {
      // nothing to do here.
    }
  ]);
